<div class='hero'>
  <section class='container'>
    <%= render 'info' %>
    <%= render 'shared/social_media' %>
  </section>
</div>
<div class='sub'>
  <section class='container'>
    <!--div id='search_form'>
      <%= form_tag(explorers_search_path, method: 'get') do %>
        <div class='input-group input-group-lg'> 
          <%= text_field_tag :search, nil, class: 'form-control', placeholder: 'Enter Block, Address or Transaction' %>
          <span class='input-group-btn'> 
            <%= submit_tag 'Search', class: 'btn btn-default' %>
          </span> 
      </div>
      <% end %>
    </div-->
    <%= render 'blocks' %>
  </section>
</div>
